// app/(auth)/auth-page.tsx (hoặc một tên file tương tự)
import AuthContainer from '@/components/Auth/AuthContainer'; // Đảm bảo đường dẫn chính xác

export default function AuthPage() {
    return <AuthContainer />;
}